name: Build and push image to AWS ECR

on:
  workflow_call:
    inputs:
      ECR_REGISTRY:
        required: true
        type: string
      ECR_REPOSITORY:
        required: true
        type: string


jobs:
  build:
    runs-on: ubuntu-latest